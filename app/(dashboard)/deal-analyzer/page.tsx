// Deal Analyzer shares the Deals page component — analysis features are built into the deals page.
// Dashboard links to /deal-analyzer?action=new and /deal-analyzer?deal=<id> which are handled there.
export { default } from '../deals/page';
